name: Docker Image CI

on:
  push:
    branches: [ init-dockerfile ]
  pull_request:
    branches: [ master ]

jobs:

#  build:
#
#    runs-on: ubuntu-latest
#
#    steps:
#    - uses: actions/checkout@v2
#    - name: Build the Docker image
#      run: docker build . --file Dockerfile --tag obyz/tayssir-covid:latest
#    - name: login into DockerHub
#      if: success()
#      run: docker login -u obyz -p ccb60341-6a21-485f-bde1-ac03d4fc32f3
#    - name: push the Docker image
#      if: success()
#      run: docker push obyz/tayssir-covid:latest

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: run App Remotly
        uses: appleboy/ssh-action@master
        with:
          host: 51.15.82.73
          username: root
          key_path: mifmif-private-key.ppk
          passphrase: Password2020!
          port: 22
          script: whoami

